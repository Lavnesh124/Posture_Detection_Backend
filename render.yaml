services:
  - type: web
    name: posture-detection-backend
    runtime: node
    buildCommand: |
      npm install
      pip install -r requirements.txt
    startCommand: node server.js
    plan: free
